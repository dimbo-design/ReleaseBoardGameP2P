# Backlog — gaps, disputes and stopgaps

The place where what is **missing, disputed or temporarily patched** is written down instead of
being quietly worked around. The other four files describe what exists and is verified; this one is
the only place allowed to describe what does not.

**The rule this file exists for.** When you hit a gap while working on an animation — no module for
a movement you need, a value you cannot reach, a rule nobody has decided — you do **not** invent a
local solution and move on. That is exactly how a movement ends up written three times in three
scenes. You either raise it and get a decision, or you write the stopgap down here, in the open,
with what it costs.

A gap that is not written down is indistinguishable from a gap nobody noticed. This file is what
turns "I ran into something" into a thing that can be scheduled.

**Two places, one finding, and that is on purpose.** The visible register is the playground
**`Interaction audit`** page (its "Требует доработок" section) — that is where the work is looked at,
so that is where a finding has to show up, in one line with a status. This file is the same finding
in full: what it costs and what would close it. Enter it in both — the page so it is seen, here so
it can be acted on.

## How to write an entry

```
### Title — what is missing, in one line
**Что не хватает.** The gap itself, concretely: file, value, movement.
**Чем грозит.** What it actually costs — a copy that will drift, a rewrite from scratch, a bug
that already happened. No vague "not clean".
**Что закроет.** The smallest thing that ends it, with the shape it should take. A recommendation,
not a plan.
**Статус.** `открыто` · `времянка` (a stopgap is in place — say what it is) · `решено` (then move
the description into the docs and delete the entry).
```

Statuses are for reading, not for bookkeeping: a `решено` entry does not stay here as a trophy — its
content moves into `reference.md` / `glossary.md` / `recipes.md` and the entry goes.

---

### Для победы последнего выжившего нет утверждённой сцены

**Что не хватает.** `gameOver(condition: 'lastStanding')` приходит после пути исключения игроков,
а `GameEndStory` задаёт только другой рассказ: последний релиз сел в зону, после чего сработали
хлопушки. У победы последнего выжившего нет ни последней посадки релиза, ни своей утверждённой
хореографии. В #107 живой борд поэтому переносит хлопушки на все победы с условием `release`, но не
выдумывает их для `lastStanding`: после такта исключения остаётся прежний переход к `GameOver`.

**Чем грозит.** Два условия победы заканчиваются с разным количеством церемонии, а визуальный язык
для `lastStanding` нельзя вывести из существующей сцены без решения владельца: повторить хлопушки,
показать последнего игрока или сделать отдельный финал — разные рассказы, которые закрепят разные
правила восприятия.

**Что закроет.** Утверждённая playground-сцена или явное решение, что `lastStanding` должен
использовать релизные хлопушки без посадки карты. После этого `planBeats` сможет поставить
соответствующий такт по условию события без догадки.

**Статус.** `открыто`.

---

### Промах Security Bug нечем показать — правило есть, такта нет

**Что не хватает.** Ничего — закрыто #105. Вопрос стоял так: владелец правил решил (22.08.2026,
`cards.md`), что запрос Security Bug публичен и при попадании, и при промахе — стол видит, какую
карту запросили, и при промахе тоже должен увидеть, какую запросили и не получили. Такта под вторую
половину не было нигде: в плейграунде показан только удачный путь — карта соперника выбирается и
открыто уезжает к атакующему (`PickSpecificCardStory`), — а промах не показан ничем, карта атаки
просто уходила в сброс.

**Чем грозит.** Ничему — задача закрыта. Пока не была: правило есть, а на экране его нет, то есть
игрок узнаёт об исходе не из стола. Хуже обычной дыры тем, что это не дефект — ничего не ломается,
ничего не мигает, просто информация, которую правила называют публичной, до стола не доезжает; такое
не находится глазами на ревью и не ловится ни одним тестом.

**Что закрыло.** Такт `runRequested` (`apps/frontend/src/features/board-beats/transferBeat.tsx`, #105).
Промах несёт весь стол, а не только атакующий: `requested` уходит без `visibleTo` и его поле `hit`
приезжает всем, поэтому на каждом пире названная карта поднимается в центре (`popIn`), стоит
`REQUEST_HOLD = 820`, цель вздрагивает — `play('shake', …, { amp: 9, dur: 460, shape: 'spring' })`, —
показывается подпись `table.requestMiss`, и через `MISS_HOLD = 1620` сцена гаснет. Ничего из этого не
переживает пендинг: на промахе он снимается сразу, так что такт несёт всю сцену целиком или стол не
узнаёт исхода. Цель вздрагивает В ТОМ ВИДЕ, в каком она нарисована: место (`seatOf`) — всем
смотрящим, собственный веер (`anchors.hand`) — самой цели, у которой места нет. Описано в
[`recipes.md`](./recipes.md#a-card-changes-hands-live-board--taker-victim-watcher-and-the-ask-before-them).

**И вторая половина того же ответа — сделана вместе с первой.** Владелец правил сказал: если система
позволяет показать список карт на выбор и отобразить в нём, на что выбирающий наводит, — это хорошо,
потому что так виден сам запрос. Публичен не только исход, но и процесс. На борде выбор идёт в
`CardCatalog`-полосе (`pages/board/[gameId]/_useRequestStaging.tsx`), а на попадании названная карта
стоит в центре у ВСЕХ: `giveCard` проецируется без гейта `mine`, и `_Board.tsx` рисует
`cardById(pending.requested)` каждому пиру, пока движок ждёт передачи.

**Статус.** `решено` (#105). Запись оставлена намеренно, как и соседняя про `drawn`: текст issue #105
и исходная формулировка этой находки до сих пор утверждают, что промаха на экране нет. Строка в
реестре находок на странице аудита (`apps/playground/stories/AnimationAuditStory`) переведена в `ok`.

### Модуль, не записанный НИГДЕ, не виден ни одной проверке

**Что не хватает.** Три вещи обязаны доезжать до доки, и все три теперь проверяются машиной:
пресет без строки в `reference.md` роняет `apps/ui/src/animations/docs.test.ts`; модуль, который
есть на странице аудита, но не упомянут в `reference.md`, роняет `apps/playground/stories/docs.test.ts`;
сцена из групп «Карты» и «Интерактив» без «живой ссылки» в `recipes.md` роняет его же. Но все три
сверяют **две точки друг с другом** — значит модуль, не дошедший ни до одной, невидим для всех
сразу.

**Чем грозит.** Ровно этим и кончилось для экранного выключателя параллакса
(`CardMotionProvider`): написан, работает в настройках стола, не значился ни на странице аудита, ни
в доках — и не значился бы дальше, потому что искать его было нечему. Опаснее обычной дыры тем, что
тесты при этом зелёные: возникает ложное чувство покрытия.

**Что закроет.** Автоматически — ничего разумного: отличить модуль от вспомогательной функции может
только человек, а перечня «всех модулей анимаций» не существует иначе как в голове. Реально
закрывает дисциплина на входе: модуль считается сделанным, когда он появился **на странице
аудита**, — оттуда его дотянет тест. Это правило, а не проверка.

**Ответ владельца.** Для этого кожаные мешки и сидят за компьютерами — чтобы всё не сломалось.

**Техническая рекомендация.** Автоматизировать тут нечего, и подменять это ещё одной проверкой не
надо: любая новая сверит те же две точки. Держим правилом на входе — модуль считается сделанным,
когда он появился на странице аудита; дальше его дотягивают уже машинные проверки. Единственное, что
стоит завести, — привычку заканчивать работу над модулем строкой на странице, а не коммитом кода.

**Статус.** `открыто`, признаётся как предел метода.

---

### `placed` — это не «карта сыграна в центр»

**Что не хватает.** Рецепт «Playing a card» и задача #96 называют парой `placed` → `discarded`
движение «из руки в центр и дальше в сброс». У события `placed` два производителя —
`fake/release.ts:177` и `fake/triggers.ts:298` — и оба кладут **Monitoring в зону релиза**, где
карта и остаётся. Центра стола в `PlayerView` нет вовсе: `decks` несёт `piles` / `events` /
`discardTop` / `discardCount` и ничего больше.

**Чем грозит.** Хореография, написанная под несуществующую последовательность. #96 успел поймать
это до кода — следующая задача может не успеть и получить сцену, которая никогда не проигрывается,
без единой ошибки в консоли.

**Что закроет.** Решение о том, ЧТО показывает центр стола в живой партии. Сыгранная атака, пока
открыто окно защиты, — самый вероятный кандидат, но это решение, а не работа: пока оно не принято,
у такта «карта ушла из центра» нет триггера (см. следующую запись). До тех пор рецепт исправлен так,
чтобы называть события, которые движок действительно шлёт.

**Ответ владельца.** В покое в центре не стоит НИЧЕГО. Центр — активная область, место разыгрывания
карт, а не место, где что-то фиксируется. Наиболее правдоподобная имитация центра — сцена
`Defense Release`.

Monitoring при разыгрывании сначала ложится в центр и только потом идёт на своё место в зоне релиза
— такого в плейграунде нет.

Другими словами: в начале хода любого игрока центр стола всегда пуст, потому что все карты
разыграны и разошлись по своим местам.

**Сцены под это не требуется.** Оба движения уже есть модулями — и приход карты в центр, и приход в
зону релиза; собирать из них демонстрацию отдельного розыгрыша на нашей стороне не нужно. Плейграунд
— это витрина, а не перечень всех возможных розыгрышей, и впихивать проход Monitoring в сцену,
собранную про другое, значит ломать её ради него.

**Статус.** `открыто` — ответ есть, остаётся сторона борда: центр там описан как проходная область
(карта проходит через него и уходит на своё место), а движения для этого уже собраны.

### Лента и куча сброса расходятся — но уже не из-за молчания

**Закрыто: молчаливое банкование.** Раньше часть карт попадала в `decks.discard` прямой записью
(`bankToDiscard`, `core.ts:156`) без события `discarded`. Двумя правками это снято целиком:

- причины `attackSpent` / `defenceSpent` были объявлены и не отправлялись — теперь отправляются
  (`bankSpent`, `fake/attacks.ts:16`), закрыто в
  [#117](https://github.com/MythHand/ReleaseBoardGameP2P/pull/117) (2026-08-18);
- `resolveDdos` (`fake/handAttacks.ts`) банковал **две** карты молча — снесённый Monitoring и
  Code Review из-под возвращённого релиза. Обе теперь логируются `discarded(reason: 'destroyed')`
  с `parent` на событие сноса, как `triggers.ts` логирует спойлы уничтоженного релиза. Закрыто в
  [#125](https://github.com/MythHand/ReleaseBoardGameP2P/pull/125).

**Что осталось, и это другая причина.** `bankToDiscard` отправляет карту, у которой заполнено
`event`, **домой в колоду событий**, а не в сброс (`core.ts:157-159`) — карта событий, стоявшая на
столе за обычную, возвращается собой. Событие `discarded` при этом всё равно уходит. Лента
называет верхом карту, которой в сбросе нет.

**Проверено.** Прогон настоящих партий (2/3/4 игрока, семь сидов), по одному действию за шаг, со
сверкой последнего `discarded` в ленте против `discardTop` проекции. До правки `resolveDdos`
расхождения шли пачками и все — после `monitoringDestroyed`. После правки осталось девять, и все
до одного объясняются уходом домой: в той же редукции, где появляется расхождение, колода событий
растёт.

**Чем грозит.** То же, чем и раньше: куча собирается из ленты (`toBoardState.toDiscardHeap`), а
`Pile` перестаёт показывать `topCard`, как только куча непуста (`Pile.tsx:106`).

**Что закроет.** Решение о том, что `discarded` вообще значит: «карта ушла из игры» или «карта
легла в сброс». Сегодня она значит первое, а борда читает её как второе. Это вопрос к спеке
событий, не правка анимаций — поэтому запись остаётся открытой, а не переписывается в задачу.

**Ответ владельца.** Не на моей стороне.

**Техническая рекомендация.** Правка одна и в движке: `bankToDiscard` шлёт `discarded` с причинами,
которые уже объявлены и не используются. Как только событие появляется, обход снимается ЦЕЛИКОМ —
подставная карта с отрицательным ключом, расхождение глубины и сверка верха с `discardTop` уходят
вместе с ним, потому что куча снова собирается из одной ленты, а не из ленты плюс догадки.

**Статус.** `времянка` — если верх кучи не совпал с `discardTop` проекции, настоящий верх
дописывается в кучу отдельной картой со скаттером по отрицательному ключу (`scatterAt(-1 - count)`,
вне диапазона id событий, чтобы подставная карта не унаследовала позу настоящей). Счётчик остаётся
проекционным, то есть число верное всегда; врёт только глубина кучи.

> Времянку **снимать нельзя**, хотя молчаливого банкования больше нет. В ревью
> [#125](https://github.com/MythHand/ReleaseBoardGameP2P/pull/125) она была отмечена как «вероятно
> снимаемая» — раз причины шлются, кучу можно собрать из одной ленты. Прогон говорит, что нет:
> расхождение осталось, только причина у него теперь другая и названа выше.

### У `neutralized` два производителя, и это два разных движения

**Что не хватает.** Причина `discarded.reason` не определяет, ОТКУДА карта летит. У
`neutralized` два источника: `triggers.ts:211` — жертва, когда релиз и приложенный к нему Code
Review уходят из зоны; и `triggers.ts:184` — Debugger, сыгранный **из руки** в ответ на 503, и это
рядовой случай. Первый уходит из слота зоны, второй руки не покидал. Причина сужает, где искать,
но ответа не даёт.

**Чем грозит.** Пока `planBeats` считал причину решающей, у более частого из двух не находилось
источника — и он не анимировался вовсе, ни у себя, ни у соперника. Ровно тот класс дефекта, который
не видно ни в одном тесте: движение просто не проигрывается, ошибок нет.

**Что закроет.** По-хорошему — событие, которое само называет место, откуда карта ушла (зона / рука
/ колода). Пока такого нет, `sourceOf` пробует зону, затем руку, затем сидушку: порядок вместо
ветвления по причине. Отдельно не решено, куда целиться приложенному Code Review — он лежит в
`support`, а не в `release`, и `slotHolding` его не находит.

**Ответ владельца.** Code Review летит со своей позиции, как лежит, БЕЗ ТЕЛЕПОРТАЦИЙ В ДРУГИЕ
ПОЗИЦИИ. У этой карты из зоны релиза есть ровно один путь — в сброс; никуда больше оттуда она
улететь не может.

**Сцена-источник на это есть.** `Error503Story` держит релиз с подоткнутым Code Review в зоне и
уводит их в сброс одним разменом, размеряя именно наклонённую вспомогательную половину, а не общую
коробку пары (`discardExchange`, там же комментарий почему). То есть в ките ответ уже выполнен;
промах — на борде, где `slotHolding` не находит карту, лежащую в `support`, а не в `release`.

**Статус.** `времянка` — порядок поиска вместо решения по причине; для Code Review вопроса «куда
целиться» больше нет: он стартует со своей позиции в зоне и уходит в сброс, как в `Error503Story`.

### Что делает такт, если цели уже нет

**Что не хватает.** Карта, которую более позднее событие того же батча убрало с доски, — правила
для неё нет. Такт не может её измерить: слота, из которого она летит, на экране уже не существует.

**Чем грозит.** Любой локальный ответ станет правилом: по нему напишут код, его закрепит тест, и
следующая сцена будет сверяться с ним как с решённым.

**Что закроет.** Решение, а не выдумка. До него `planBeats` просто не строит полёт для карты без
источника — ровно как для события, у которого хореографии нет вообще. Это самый узкий из возможных
вариантов: анимации нет, исход прежний (карта в сбросе по проекции). Ни запасного источника, ни
угаданного прямоугольника, ни предупреждения в консоли, которое выглядело бы как принятое решение.

**Ответ владельца.** Вопрос некорректный. Потерянных карт на экране быть не может: карта всегда
где-то в игре — в руке, в зоне релиза, в колоде добора или в сбросе. Общая сумма карт по всем этим
местам всегда одна и та же; никаких потерь и никаких сбитых позиций в колодах.

**Статус.** `открыто` — не как «правило для карты без источника», а как вопрос к самому случаю: он
описан от устройства батча, а не от игры. Пока не показано, что карта может остаться без места на
экране, у записи нет предмета.

---

### `drawn` был приватным, и добор соперника было нечем анимировать

**Что не хватает.** Ничего — вопрос закрыт этой задачей (#97, wave 1). Issue #97 формулировал его
так: «`drawn` не несёт `card`, когда добирает не этот игрок, — это ровно ветка рубашкой вверх,
проекция уже нужной формы». Формулировка была неверной: `fake/reduce.ts:112-128` слало на обычный
добор `drawn { player, card, pile, deckSize, visibleTo: [drawer] }`, и `visibleTo` роняло всё
событие целиком у каждого, кроме доборщика (`network/session/audience.ts`) — соперник не получал
вообще ничего, только тик `handCount` в проекции. Ветке «карта уходит к месту соперника» было не на
чем сработать.

**Чем грозит.** Ничему — задача закрыта. Запись держится здесь не как дыра, а как исправление: кто
прочтёт исходный текст issue первым, должен найти опровержение рядом с ним, а не переоткрывать
вопрос по новой.

**Что закроет.** Закрыто. `reduce.ts` больше не ставит `visibleTo` на обычный добор — событие
публичное для всех. Секретность личности карты переехала в движок: `@release/engine` экспортирует
`redactFor(event, viewerId)`, вырезающую `card` из чужого `drawn` и не трогающую остальные события;
`forViewer` сперва фильтрует по `visibleTo`, как и раньше, затем прогоняет уцелевшее через
`redactFor`. `audience.ts` по-прежнему не читает карту из полезной нагрузки — правило ему выдают, а
не он его выводит.

**Статус.** `решено` — запись оставлена как есть, вопреки обычному правилу этого файла (перенести
содержимое в доки и удалить запись): текст issue #97 продолжает утверждать обратное, и опровержение
должно стоять рядом с ним, а не только в коде.

---

### `pilesChanged` не называет ни операцию, ни индекс разделения

**Что не хватает.** Событие несёт только счётчики (`piles: number[]`) — ни какая операция
произошла (сплит / мёрдж / добор из сброса / вычерпывание), ни какая стопка в ней участвовала.

**Чем грозит.** Без вывода нечем решить, какой пресет играть и откуда лететь: `flyFrom` для сплита
целится в конкретный индекс, `absorbToDeck` для мёрджа — во все стопки, кроме выжившей. Угадывание
на месте, на такте — ровно то, из-за чего одно и то же движение потом переписывают в трёх разных
местах по-разному.

**Что закроет.** Само событие могло бы называть операцию и индекс явно — это решение движка, а не
то, что решается внутри анимационной задачи.

**Ответ владельца.** Не на моей стороне.

**Техническая рекомендация.** Событие называет операцию и индекс, такт читает вместо вывода. Пока
этого нет, вывод по числам держится на том, что у разных операций разная форма счётчиков — а на
колодах Git Branch / Git Merge формы совпадут (сплит и мёрдж дают одну и ту же длину результата), и
разбирать их придётся ровно там, где стопок несколько.

**Статус.** `времянка` — `classifyPiles` (`apps/frontend/src/features/board-beats/planBeats.ts`)
выводит операцию позиционно, сверяя `before`/`after` по длине и по сумме: `after.length ===
before.length + 1` и `before[i] === after[i] + after[i+1]` → сплит по `i`; `after.length === 1` при
`before.length > 1` → мёрдж (сумма пришла больше суммы стопок — значит, зашёл ещё и сброс); пила,
добавленная в хвост при пустом изменении существующих, → второй шаг Git Branch + Sudo. Порядок
проверок важен: вычерпывание (`[0, 10] → [10]`) и мёрдж дают одну и ту же длину результата, и
вычерпывание проверяется первым, иначе спутаются. Само вычерпывание (prune) не даёт `PileStep` —
пропавшей пустой стопке нечего анимировать на экране — `classifyPiles` возвращает для него `null`,
а не заводит для него отдельный вид шага.

---

### ЗАКРЫТО: Сколько триггер стоит в центре — значения не было

**Что не хватает.** Внутри такта добора вскрытый триггер держится на столе `REVEAL_HOLD = 900`
(`apps/frontend/src/features/board-beats/drawBeat.tsx`) перед уходом в сброс. Источника у этого
числа нет: `DrawCardStory` держит `AI_HOLD = 4000`, но это пауза AI-ветки (стол читает эффект),
не обычного вскрытия — а для голого reveal без AI подтверждённого значения в сцене нет вовсе.
`REVEAL_HOLD = 900` — число этой задачи, ничем не подтверждённое.

**Чем грозит.** Значение может оказаться короче или длиннее того, что утвердят остальные холды
стола, и тогда его придётся переигрывать заново — не потому, что оно плохо смотрится сейчас, а
потому что оно ничем не подтверждено.

**Что закроет.** Решение в рамках кросс-катной проходки по таймингам —
[#84](https://github.com/MythHand/ReleaseBoardGameP2P/issues/84).

**Ответ владельца.** Значение берётся из сцены-примера, а не подбирается в задаче. Сцена-пример
здесь — `AiCardsStory` (страница `AI cards`): поведение карт AI в центре — её предмет. `DrawCardStory`
не источник для этого холда, её предмет — колоды добора, поэтому её `AI_HOLD = 4000` не спор со
сценой AI, а число про другое.

**Найденное значение.** `TABLE_HOLD = 2600` — столько вскрытая AI-карта стоит открытой, пока стол её
читает; всё это время вскрытый триггер стоит рядом как причина, и уходит в сброс вместе с ней
(`resolveGeneric`). У Галлюцинации холд вдвое длиннее — `HALLUCINATION_HOLD`. То есть отдельного
«голого» вскрытия без AI, у которого был бы свой холд, в сценах нет: триггер уходит в сброс как
часть разбора AI-карты.

**Чем закрыто.** Правка на борде сделана. `REVEAL_HOLD = 900` больше не существует:
`drawBeat.tsx` держит вскрытый (не-AI) триггер на `TABLE_HOLD`, импортированным из
`features/board-beats/toCentre.ts` — того же модуля, который завела AI-карта на этой ветке
(`aiBeat.tsx` держит свою пару им же). Одно число, оба читателя; расходиться больше нечему.

**Статус.** `закрыто`. Строка в реестре находок на странице аудита
(`apps/playground/stories/AnimationAuditStory`) переведена в `ok`.

---


### `drawBeat` меряет якоря без `nextFrames`, и мёрдж внутри батча уронит добор

**Что не хватает.** `toCentre` в `apps/frontend/src/features/board-beats/drawBeat.tsx` (около
строки 102) меряет `anchors.pileBox(d.pile)` и `anchors.centre` на входе в такт **без
`await nextFrames()`** перед измерением. Опасность в полном виде: батч-эффект `useBeats` —
layout-эффект, так что к моменту, когда `drain()` зовёт раннер, DOM ещё держит `live` — проекцию,
которую батч только что произвёл. Тень, откуда такт должен уходить, встанет на своё место одним
коммитом позже. `discardBeat.tsx` подробно документирует это и платит `await nextFrames()`;
`deckBeat.tsx` получил тот же вызов после того, как ревью нашло: без него мёрдж не анимировал
ничего — `pileBox(i)` каждой слившейся стопки отвечал `null`, цикл поглощения `continue`d на всех,
и `Promise.all([])` разрешался, не сдвинув ни одной карты.

**Чем грозит.** `drawBeat` сходит с рук СЕЙЧАС только потому, что добор никогда не убирает стопку:
после `drawn` число карт в стопке меняется, но сама стопка остаётся на том же индексе и на том же
месте, так что измеренный прямоугольник верен по случайности, а не по построению. В момент, когда
батч совмещает добор с исчезновением стопки — `[drawn(pile 2), pilesChanged → merge]` —
`pileBox(2)` на входе ответит `null`, и **добор пропадёт целиком**. Такой батч недостижим сейчас:
стопка пропадает только через Git Branch / Git Merge, а #61 ещё не сделал их играбельными с борда.
#108 — это как раз та волна, которая это меняет.

Отдельно: стаб анкоров в `drawBeat.test.tsx` отвечает узлом на любой индекс — та же форма стаба,
которая пропустила деково баг через ревью. `deckBeat.test.tsx` был переработан отвечать `null` вне
текущего ряда; тесты добора — нет.

**Что закроет.** `await nextFrames()` в начале `toCentre`, тем же приёмом, каким это закрыто в
`discardBeat.tsx` и `deckBeat.tsx` — измерение якорей после того, как тень встала на экран, а не в
момент, когда DOM ещё держит `live`. Заодно стаб анкоров в `drawBeat.test.tsx` стоит привести к
форме `deckBeat.test.tsx` — `null` вне текущего ряда, — иначе тот же класс бага снова пройдёт
незамеченным.

**Ответ владельца.** У добора есть логика исчезновения стопки, и она предусмотрена: на плейграунде
этот случай разобран — как это должно выглядеть на нескольких колодах. Плюс отдельная логика на
последнюю стопку: сброс превращается обратно в колоду добора, потому что **колода добора на столе
должна быть всегда одна**. Исключение — когда карт не осталось вовсе (все на руках); этот случай
уже где-то описан в доках.

**Что из ответа следует.** Запись перестаёт быть латентной. «Недостижимо до #108» держалось на
посылке, что стопка исчезает только через Git Branch / Git Merge; если стопки исчезают и сами по
себе, а на последней сброс становится новой колодой, то батч «добор + исчезновение стопки» —
рядовой ход партии, а не будущий край. Значит и цена другая: не «когда-нибудь всплывёт», а «добор
молча пропадёт на обычном ходу».

**Техническая рекомендация.** Дописать `await nextFrames()` в начале `toCentre` сейчас — это одна
строка и тот же приём, что уже оплачен в `discardBeat` и `deckBeat`; ждать под это отдельной волны
нечего. Вторым шагом привести стаб анкоров в `drawBeat.test.tsx` к форме `deckBeat.test.tsx`
(`null` вне текущего ряда), иначе тест продолжит пропускать ровно этот класс. Правка во фронте.

**Правило уже в спеке.** [`resolution.md` §8](../rules/resolution.md#8-добор-и-триггеры), «Колода
кончилась во время добора», разводит оба случая: одна из нескольких колод опустела — добор
доводится до конца, и только потом пустая перестаёт существовать, а оставшиеся сдвигаются; забрана
последняя карта и колод не осталось вовсе — сброс перемешивается и становится новой колодой добора.
Там же и предел: чтобы кончились и колоды, и сброс, карты должны исчезнуть из партии, чего в живой
игре не бывает.

То есть исчезновение стопки — не край, а описанный ход: **`pilesChanged` с уменьшением числа колод
приезжает следом за обычным добором**, и это ровно тот батч, на котором такт теряет карту.

**Статус.** `открыто` — случай достижим (ответ владельца), правка на борде не сделана, подстраховки
на месте нет.

### Сброс в паре `[drawn(mine), discarded]` может целиться не в тот слот руки

**Что не хватает.** В батче `[drawn(mine), discarded]` `planBeats` резолвит `source.index` сброса
против `before` — веера до батча, — но очередь тактов теперь передаёт дальше опубликованное
состояние такта добора как базу такта сброса, а `useHandArrival` вставляет прилетевшую карту в
СЕРЕДИНУ веера. Сброс, чей индекс лежит на месте вставки или после него, летит из слота, соседнего
с правильным, а не из самого правильного.

**Чем грозит.** Пока только косметика, и строго лучше того, что было: до фикса с передачей базы
дальше между тактами весь веер на глазах откатывался к N картам между этими двумя тактами. Честный
фикс — чтобы планировщик резолвил индексы слотов против состояния, на котором такт реально
исполнится, а это меняет то, как `planBeats` и очередь делят ответственность между собой, — не
однострочная правка, поэтому запись, а не тихая доработка на месте.

**Что закроет.** Решение о том, как `planBeats` резолвит индексы слотов относительно состояния
такта, которое их использует, а не относительно `before` батча.

**Ответ владельца.** Вопросом: это какой-то крайний тест на анимации? Зачем совмещать добор и
сброс? Когда это вообще встречается?

**Что отсюда следует.** Прежде чем решать, как резолвить индексы, надо показать саму ситуацию: в
какой момент партии добор и сброс приезжают одним пакетом. Пока это не названо игровым случаем,
запись описывает устройство батча, а не то, что видит игрок.

**Статус.** `открыто`.

### У таймера хода три хвоста: жесты, предупреждение, PASS под своим решением

**Что не хватает.** Таймер хода живёт (30 сек на действие — `TURN_ACTION_MS`, штамп в post-step
`reduce`; хранитель доигрывает минимальный ход по истечении), но три вещи из дизайн-заметок дока не
сделаны. (1) Заметки обещали продление на «осмысленное действие» — прицел, перетаскивание карты;
движок штампует часы только на ПРИНЯТОЕ действие, жест без действия для него невидим. (2)
«Предупреждение на последних секундах» не построено: в фазах draw/push канал `danger` у `TurnDock`
визуально ничего не меняет, так что дожать нечем. (3) Во время СВОЕГО pending док по-прежнему
показывает клавишу PASS (`reaction`), которую движок отвергает, пока решение открыто, — та самая
живая мёртвая клавиша, которую `hold` закрыл для чужих pending и своего релиза под окном; выбор
игрок делает в `PendingPrompt`, клавиша — шум.

> Половина (3) закрыта в #101 (Fix B): `discardForRelease`, свой pending, получил собственную фазу
> `cost` в `TurnDock` — своё слово («цена релиза» / «сбросьте карту»), акцент потери и НИ ОДНОЙ
> клавиши. Не через `hold`, как предлагалось ниже: `hold` без `activePlayer` подписан «релиз под
> ударом», а цена релиза — не удар. Остальные свои pending (`defend` и прочие) по-прежнему рисуют
> живую мёртвую PASS.

**Чем грозит.** (1) игрок, целящийся атакой двадцать пятую секунду, получает авто-добор под руку —
таймер читается как враждебный; (2) авто-ход наступает без предупреждения и выглядит багом, а не
правилом; (3) клики в PASS уходят в молчаливые реджекты — ровно то «нажал и ничего», с которого
начался #98.

**Что закроет.** (1) решение, а не код: либо признать «продлевают только принятые действия»
правилом и вписать в заметки дока, либо придумать честный канал для жестов (спорно — это не
действие движка); (2) ветка в `accentFor` + переключение на danger при малых `seconds` в draw/push;
(3) остаток — то же, что уже сделано для `cost`: у каждого своего pending, где PASS отвергается,
свой честный вид дока. Для `defend` это не «убрать клавишу», а решить, чем она становится:
отказ от защиты — это `RESOLVE {kind:'defend', card:null}`, и одобренная сцена
(`DefenseReleaseStory`) вешает его именно на PASS дока. Борд сейчас держит отказ отдельной
кнопкой в строке-подсказке под центром (#101, Fix B), а клавиша дока осталась мёртвой.

**Ответ владельца.** Предупреждения на последних секундах не заводим — избыточного акцента
поверх кораллового дока не нужно, оставить как есть.

Продлевать таймер на жесты не нужно: в пошаговой игре время на ход фиксировано независимо от
того, чем занят игрок, и «отошёл за кофе» не должно его растягивать. Таймер — отдельный слой
логики; это общий вектор, а не окончательное решение по нему.

**Статус.** `открыто` — два хвоста из трёх закрыты решением (предупреждение и продление не нужны);
остаётся мёртвая клавиша PASS под своим решением.

### Жест прицеливания на сенсорном экране не решён

**Что не хватает.** Стрелка (#99) целится курсором: `useArrow` следит за `mousemove`, а вся стадия
жеста — `_useBoardStaging.ts` — устроена вокруг мыши. `onHandPlay` продолжает полёт из
`HandPlayDrop.rect`, который даёт мышиный порог клика/drag в `Hand`, и сам прицел стрелки — трасса
за курсором, а не за касанием. Ни здесь, ни где-либо ещё в проекте жест для тач-экрана не решён —
issue #99 называет это своей же нерешённой границей, а не чем-то, что закрывает эта задача.

**Чем грозит.** На сенсорном экране карту с целью нечем даже поставить в центр стола — постановка
ломается раньше прицела: нет курсора, за которым стрелке следить, и нет события, которое
`_useBoardStaging` приняла бы вместо `mousemove`. Пока экран остаётся мышиным по факту, а не по
решению — и это тем незаметнее, что все тесты (клавиатура, мышь) зелёные.

**Что закроет.** Решение, каким жестом на сенсорном экране заменяются постановка и прицел
(задержанное касание? отдельная кнопка-цель?), и сцена, которая его показывает, — раньше кода под
него, а не как побочный эффект следующей задачи по борду.

**Ответ владельца.** Решается отдельным заходом. Направление: удержание пальца на экране
указывает, куда прилетит, и срабатывает на выбранном объекте, когда палец отпущен.

**Статус.** `открыто` — отдельный заход, направление задано.

### Отбить атаку с клавиатуры больше нечем — последний островок убран сознательно

**Что не хватает.** До #101 (Fix B) панель `PendingPrompt` рисовала для `defend` список карт
кнопками (`role="option"`, настоящие `<button>`), и это был **единственный** способ ответить на
атаку с клавиатуры. Панель на `defend` снята: она перекрывала ту самую атаку, о которой спрашивала
(непрозрачная поверхность на z 92 над слотами центра z 9–11), и спрашивала второй раз то, на что
веер уже отвечает жестом. Вместе с ней ушёл и островок доступности.

Убрано осознанно, а не по недосмотру: веер и без того мышиный целиком — `Hand` рисует карты
`interactive={false}`, слоты это `<div>` без `tabIndex` с одним `onMouseDown`, и собственный
biome-ignore в `Hand.tsx` говорит прямо: «pointer-only … no keyboard affordance implied». То есть
ни один ход на борде — ни розыгрыш, ни прицел, ни фолд пары — с клавиатуры не делается, и `defend`
был исключением, а не правилом. С клавиатуры на борде осталось достижимым: отказ от защиты (кнопка
в строке-подсказке), клавиши дока, рейл и панели.

**Чем грозит.** Игрок, который не может пользоваться мышью, теперь не может ни атаковать, ни
защищаться — раньше он не мог только атаковать. Разница в том, что защита обязательна по темпу:
пендинг идёт с дедлайном, и «не успел» кончается пропущенным ударом, а не пропущенным ходом. Плюс
это ровно тот класс дыры, что и запись про сенсорный экран выше: тесты зелёные, потому что они
кликают мышью.

**Что закроет.** То же решение, что и для сенсорного экрана, и лучше одним заходом: каким
не-мышиным способом на борде выбирается карта в веере. `Hand` — общий компонент, так что ответ
нужен один (фокусируемые слоты + Enter/пробел + стрелки?), а не отдельный костыль под `defend`.
Возвращать список карт в панель как «клавиатурную дверь» — не ответ: он воспроизводит и перекрытие
центра, и второго спрашивающего.

**Дописано после ревью всей ветки (#101, Fix C).** Дыра шире, чем «нечем отбиться»: оплата релиза
тоже отвечается ТОЛЬКО мышью — панель для `discardForRelease` снята по той же причине, что и для
`defend` (карты на столе уже спрашивают), а веер по-прежнему без клавиатуры. И именно это сделало
блокер этого захода блокером: комбо-релиз (Code Review) оставлял `staged.merged` на всю фазу
оплаты, веер уходил в `pointer-events: none`, и цена становилась неоплачиваемой НИКАКИМ вводом —
не только мышиным. Мышиную половину починил Fix C (гвард уступает шагу, который ждёт веер);
клавиатурной двери как не было, так и нет, поэтому у любого такого дедлока по-прежнему нет второго
выхода. Это повышает приоритет записи, а не меняет её содержание.

**Дописано в #104.** К `defend` и `discardForRelease` добавился третий случай — сброс по лимиту
руки. Панель `PendingPrompt` для `handLimit` снята по той же причине (карты на столе спрашивают
сами, а панель накрыла бы сетку, которую игрок в этот момент собирает), и веер по-прежнему без
клавиатуры. Этот случай хуже двух предыдущих: у `handLimit` **нет дедлайна**
(`apps/frontend/src/network/session/referee.ts` — «`handLimit` и `crush` не несут своего
дедлайна»), так что зависший выбор не разруливается таймаутом, а останавливает матч всем. Запись
та же, приоритет выше.

**Ответ владельца.** Игра изначально сделана под мышь; клавиатуры в планах не было. Возвращать
островок ради одной защиты не нужно — добавление клавиатуры обсуждается и прорабатывается
отдельным заходом.

**Статус.** `закрыто` — потеря принята: игра мышиная по замыслу.

### Кому Rollback вернул атаку — выводится, а не читается

**Что не хватает.** `attacks.ts:245-252` кладёт атакующую карту в руку прямой записью и не шлёт
ни одного события — `handTransfer` объявлен в `events.ts:37` и здесь не используется. Такт
(`defenseBeat.runCovered`) выводит получателя: защитник, если в этой же резолюции есть
`discarded(support-sudo, defenceSpent)`, иначе атакующий.

**Чем грозит.** Движение без события за спиной ломается молча: переименование причины сброса или
второй sudo-способный support в каталоге — и возврат просто перестаёт играться, а тесты на
`planBeats` этого не увидят, потому что они пиннят тот же вывод.

**Что закроет.** Движок шлёт `handTransfer` при возврате, план читает его вместо вывода. Правка в
`packages/engine`, не в анимациях.

**Ответ владельца.** Не на моей стороне. И как запись бэклога это негодно: «кто-то что-то
переименует, и логика сломается» верно для чего угодно, а не для этой ситуации. В том и смысл
архитектуры, что если кто-то решил страдать хернёй, у него игра сломается, и сопартийцам будет
неприятно. Вопрос про абстрактную общую ситуацию подан через частный случай так, будто частность
делает его важным, — и мимо того, что решение давно продумано за рамками вопроса.

**Что из записи остаётся.** Аргумент про переименование снят — он общий и ничего не про этот такт.
Факт остаётся один, проверяемый: движок кладёт возвращённую карту в руку прямой записью, а
объявленный `handTransfer` не шлёт никогда, поэтому получателя такт выводит. Это и есть предмет
записи; всё остальное из «чем грозит» — риторика.

**Техническая рекомендация.** Движок шлёт `handTransfer` (он уже объявлен в `events.ts` и нигде не
используется) при возврате карты в руку, такт читает получателя из события вместо вывода. Тесты на
план при этом надо переписывать вместе с правкой: сегодня они пиннят ТОТ ЖЕ вывод, поэтому поломку
не увидят.

**Статус.** `времянка` — вывод на месте и покрыт тестами (`planBeats.test.ts`, `defenseBeat.test.tsx`).

### Отмена пары возвращается в одну щель веера, а не в две

**Что не хватает.** `cancel()` в `_useBoardStaging.ts` возвращает обе половины сложенной пары ОДНИМ
вызовом `useHandArrival.arrive`, на индекс, где опора стояла в момент вытягивания, — общая щель на
двоих, а не своя у каждой половины. Так же устроен и `cancelStage` в ComboStory, откуда это
портировано без изменений.

**Чем грозит.** Пока только косметика: обе карты садятся в одно место вместо двух исходных слотов, а
веер довключается в порядок проекции только после того, как `staged` очищается целиком. Собственное
принятое поведение сцены-источника, а не что-то придуманное отдельно для борда.

**Что закроет.** Решение о двух независимых щелях вместо одной — но предметное, только если возврат
пары станет плохо читаться на живом борде; до тех пор менять нечего.

**Ответ владельца.** Кейса отмены сложенной пары нет: в момент, когда взята вторая карта для
комбо, пара разыгрывается. В руку по ходу партии всегда приходило по одной карте.

Отдельно — как механика вставки в руку: две карты должны ложиться каждая в своё окошко, рядом.
Кейс на это есть на странице `Card to Hand`; похоже, он и так работает — надо проверить.

**Проверено.** Работает, и в ките, и на экране. Веер раскладывается как на `n + gapSize` карт и
оставляет пустыми слоты `gapAt…gapAt+gapSize-1` (`table/Hand/Hand.tsx`), где `gapSize` — число
прилетающих карт; каждая летит в СВОЙ слот, `slotPlacement(gap + i, total)`
(`animations/useHandArrival.tsx`), со своим углом и своей конечной точкой. Одна щель на двоих
получилась бы, только если бы обе карты целились в `gap`. На странице `Card to Hand` пара садится
в два соседних окошка.

**Статус.** `закрыто` — отмены пары как случая нет, а парная вставка в руку делает то, что от неё
требуется.

### Атака на борде прилетает без наклона — наклон даёт поза покоя, а не посадка

**Что не хватает.** В одобренной сцене (`DefenseReleaseStory`) атака летит с места соперника
`play('playToCenter', …, { rotate: ATTACK_POSE.rot })` — 480ms, наклон едет вместе с картой, и она
ПРИЗЕМЛЯЕТСЯ уже наклонённой. На борде тот же прилёт делает `comboBeat.foldIn` — `foldIntoPair`,
620ms, только translate+scale: `foldIntoPair` по своей подписи умеет позу покоя половины ВНУТРИ
пары, но не поворот всей карты на столе. Один рантаймер обслуживает и одиночную атаку, и пару с
судо, и переписывать парный путь ради одиночного — хуже, чем текущее положение. Поэтому наклон
даёт не посадка, а поза покоя: `_Board.tsx` рисует pending-атаку во внутреннем `.pose` при
`restTransform(ATTACK_POSE)` (#101, Fix A, Defect 2 — до этого атака стояла ровно, а `useDiscardExit`
получал `pose: ATTACK_POSE` как «наклон, ОТ которого уходит», и карта дёргалась с 0° на −4° на
первом кадре ухода).

**Чем грозит.** Расхождение остаётся видимым в движении, хотя покой теперь совпадает: в сцене карта
доворачивается по дуге полёта, на борде — встаёт ровно и наклон появляется мгновенно в кадре, где
флаер снимается и статичный рендер берёт своё. На тихой руке это читается как микро-щелчок. И это
ровно тот класс расхождения, который правило анимаций просит не разводить: одно движение, две
формы, и ничто, кроме внимания, не держит `ATTACK_POSE` одинаковым по обе стороны — значение живёт
в двух файлах (`entities/game/board/poses.ts` и константа сцены).

**Что закроет — половина уже сделана.** Решение владельца правил: наклон это не украшение, а
единственное, что на столе говорит «карту положила рука». Разложенное системой в центр — добор,
вскрытый триггер, карта из колоды событий — ложится **ровно**; пришедшее из руки игрока — **под
своим углом**. Записано инвариантом **I11** в [README](./README.md#global-invariants), потому что
это правило про все сцены, а не про одно движение.

Под него в словарь добавлен шаг **`landInPose`** (`apps/ui/src/animations/presets.ts`, строка в
[`reference.md`](./reference.md), карточка на странице `Animations`): FLIP-форма, как у
`foldIntoPair`, но `pose` — поза САМОЙ карты на столе. Наклон едет вместе с картой и в неё же
приземляется. `foldIntoPair` растягивать не стали: пара и посадка на стол — разные ходы, и одно имя
на оба смысла заставляло бы читателя каждый раз выяснять, что складывается.

Осталось: `comboBeat.foldIn` вызывает `landInPose` для ОДИНОЧНОЙ атаки вместо `foldIntoPair`, и
поза покоя перестаёт быть компенсацией. Правка во фронте, не в ките.

**Ответ владельца.** На нашей стороне уже решено — надо адаптировать.

**Техническая рекомендация.** В `comboBeat.foldIn` одиночная атака (без судо) едет через
`landInPose` с `pose: ATTACK_POSE`, парный путь остаётся на `foldIntoPair` — переписывать пару ради
одиночной нельзя, это разные ходы. После этого поза покоя в `_Board.tsx` перестаёт быть
компенсацией: карта приземляется уже наклонённой, а покой просто совпадает с последним кадром.
Заодно `ATTACK_POSE` стоит свести к одному объявлению — сегодня значение живёт и в
`entities/game/board/poses.ts`, и константой сцены, и одинаковыми их держит только внимание.

**Статус.** `времянка` — модуль есть, борд на него ещё не переведён; поза покоя проставлена и
совпадает с тем, от чего уходит выход (`boardComponent.test.tsx` пинит и то, и что наклон живёт на
внутреннем узле, а не на измеряемом — **I6**). Строка в реестре находок на странице аудита
(`apps/playground/stories/AnimationAuditStory`) заведена, статус `rework`.

### Один sync-флаш: тень публикуется всем, кроме того, кто сам отвечает

**Что не хватает.** Когда бросок и ответ на него приезжают ОДНИМ батчем — в звезде это норма для
каждого пира, кто не атакующий и не защищающийся, — `planBeats` теперь ведёт `openAttack` по ходу
разбора (как уже вёл `piles`), и такт `covered` строится. Но у такта есть вторая половина: атака
должна быть НА ЭКРАНЕ, пока над ней держат защиту. `runAttack` снимает свой флаер сразу после
фолда, рассчитывая, что статичный рендер центра подхватит карту тем же коммитом, а в одно-флашевом
батче такого рендера нет — `base` старше батча. Поэтому `runAttack` публикует тень с
`pending: defend`.

Не публикует в одном случае: когда отвечать должны МЫ (`plan.target === ctx.base.selfId`).
`options` здесь не вывести — движок редактирует их для всех, кроме владельца, — и пустой список
сказал бы нашему же борду «с тебя защита», не предложив ни одной легальной карты. Обоснование, по
которому это безопасно, — вывод, а не факт из событий: пир, который ответил, обязан был увидеть
атаку раньше, иначе ему нечем было отвечать; значит для него батч не одно-флашевый и ветка
недостижима. Вывод верен для сегодняшнего движка.

**Чем грозит.** Если релей когда-нибудь начнёт склеивать батчи иначе (авто-пас по дедлайну, бот,
реджойн с реплеем всей партии), ветка станет достижимой — и тогда у защищающегося атака мигнёт и
пропадёт ровно на те ~1.2s, пока над пустым слотом держат чужую защиту. Молча: тестов, которые
ловят «карта пропала с экрана», в этом классе нет, есть только «такт сыграл».

**Дописано в Fix D (#101).** У того же такта был ещё один угол, который Fix C не увидел: свой
собственный бросок. Ветка атакующего (`handoff.release()`) выходила из `runAttack` РАНЬШЕ публикации,
так что в склеенном батче — медленный канал, реджойн, реплей — его собственная атака мигала и
пропадала ровно так же. Здесь редактирование `options` ни при чём (отвечать он не может по
определению), поэтому угол закрыт: публикация вынесена в общий `standAttack()` и зовётся из обеих
веток, до того как носитель отпускает карту. Заодно публикация теперь ОТКАЗЫВАЕТСЯ поверх любого
стоящего пендинга, а не только `defend`: `...ctx.base` перезаписал бы чужой вид выдуманным. Остаётся
открытым только угол защищающегося — тот, где мешает редактирование.

**И у выдуманного пендинга выдуманные часы.** `runAttack` публикует `openedAt: 0, deadline: 0`, а
опубликованная тень — это то, что рисует ВЕСЬ борд: `deriveDock`'s `clock()` (`dock.ts`) отдаёт
`seconds: 0`, и каждый смотрящий пир видит кольцо `hold` с надписью `0s` — истёкший отсчёт по
решению, которое только что открылось, на всю длину такта. Честное чтение — не класть эти поля
вовсе: `deriveDock` спрашивает рантаймовым `'deadline' in pending`, а `clock()` рисует плоское
кольцо, когда границы нет, и других читателей у этих двух полей нигде нет. Написать это нельзя:
`defend` объявляет оба поля обязательными и обязан — `engineContract.test-d.ts` держит union
пендингов кита структурно ТОЧНО равным `PendingView` движка, где часы есть всегда; ослабить кит под
эту времянку значит ослабить контракт и для настоящей проекции, а протащить безчасовой объект
кастом — положить ту же ложь на слой глубже. То есть это тот же аргумент, что и ниже, с другой
стороны: такт — единственный производитель пендинга, который не движок.

> **Эта половина закрыта, и не заплаткой.** Владелец правил решил, что соперник **вообще не видит
> чужих часов**: пока стол ждёт чужого решения, док называет, чьё оно, и показывает пустое кольцо —
> чужой отсчёт не его время, а число, на которое он не может повлиять, только дёргается перед ним,
> пока он ждёт. `deriveDock` больше не читает часы у чужого пендинга (`dock.ts`, ветка `hold`;
> `isCounting` зеркалит), и `0s` исчезает сам: тень публикуется всем, кроме владельца, значит
> каждый, кто её видит, попадает в ветку без часов. Врать полями `openedAt: 0, deadline: 0`
> по-прежнему некрасиво, но читателя у них больше нет.

**Что закроет.** Осталась одна половина — ветка защищающегося, где тень не публикуется вовсе.
Событие или поле, из которого `options` выводится на месте (сегодня их знает только владелец
пендинга), — тогда публиковать можно всем одинаково и ветка исчезает. Дешевле и честнее по смыслу:
`runAttack` публикует тень БЕЗ `player` — отдельная, не-`pending` форма «на столе лежит атака»,
которую рендер центра умеет читать, а `answering` и док — нет. Правка во фронте, не в ките.

**Ответ владельца.** Когда кто-то кого-то атакует, эти разыгранные карты в центре стола видны ВСЕМ
— это публичное действие. Выдумывать скрытие там, где его быть не должно, не надо.

Отдельно про сам вопрос: чем защищающемуся защищаться от атаки, которой он не видит? Сцена
плейграунда показывает атаку со стороны игрока, на которого она прилетела. Ситуация выдумана.

**Что из ответа следует.** Ветка «защищающемуся не публикуем» неверна по существу, а не спорна:
защищаться от того, чего не видишь, нечем. Скрытие появилось не по замыслу, а как побочный эффект
формы — тень публиковалась как ПЕНДИНГ, а у пендинга есть список легальных карт, который движок
редактирует под каждого зрителя, и владельцу отдать пустой список было нельзя.

**Техническая рекомендация.** Разделить два смысла, которые сейчас слиты в один объект: «на столе
лежит карта» (публичное, видят все, читает только рендер центра) и «с тебя решение» (личное, с
опциями и часами, читают док и жесты). `runAttack` публикует первое — без `player`, без опций, без
часов, — и публикует одинаково всем, включая защищающегося. Вместе с этим уходит и выдуманное
`openedAt: 0 / deadline: 0`: часам просто неоткуда взяться в форме, которая их не носит.

**Статус.** `времянка` — тень публикуется там, где она нужна и безопасна; один угол закрыт гвардом
и выводом, а не событием. Строка в реестре находок на странице аудита
(`apps/playground/stories/AnimationAuditStory`) заведена, статус `rework`.

### Фолд карты и судо в пару — один ход, четыре копии

**Что не хватает.** Ход «поднять флаер/узел с двумя половинами → выставить обеим входные позы
`enterPose` → `await nextFrames()` → параллельный `foldIntoPair` на каждую половину, аукс со `snap`
в `PAIR_AUX_POSE`» написан отдельным кодом четыре раза: `DefenseReleaseStory.tsx`'s `mergeIntoPair`,
`comboBeat.tsx`'s `foldIn` и `_useDefenseStaging.tsx`'s `onCardClick` — на флаере (`useFlyer.raise`)
с `<CardPair>` как `content`; `_useBoardStaging.ts`'s разбор клика по партнёру — тот же ход, но на
персистентном узле (`pairRef`), а не на флаере. Модуля под него нет ни в одном виде — каждая копия
написана заново, а не вызвана.

**Чем грозит.** Ровно то, о чём предупреждает правило анимаций: движение, встретившееся в двух
сценах, — это модуль, который ещё не собрали; здесь их уже четыре. Правка тайминга, позы или
порядка кадров в одной копии не долетит до трёх остальных сама — включая разъезд между
`_useBoardStaging.ts` и `_useDefenseStaging.tsx`, разными сторонами одной и той же партии (атака и
защита).

Отдельно, но по тому же следу: у флаерной формы (три копии из четырёх) есть слепое пятно, которого
нет у персистентного узла (`_useBoardStaging.ts`) — `useFlyer.raise` сама дожидается
`await nextFrames()`, прежде чем вернуть элемент, поэтому первый кадр-другой рисуется в позе ПОКОЯ
(identity), а не во входной. Персистентный узел этого избегает, держа карту на `opacity: 0`, пока
входные позы не проставлены, — это единственное, что персистентный узел реально покупает у флаера.

**Что закроет.** Шаг в `apps/ui/src/animations/`, рядом с `useHandArrival`/`useDiscardExit` — те
были собраны туда тем же путём, который описывает `reference.md`'s «The movement steps»: были
разбросаны по сценам и борду, и ничто не держало копии в шаге. Подпись близкая к ним: пара карт +
rect назначения → `nextFrames` → `foldIntoPair` на каждую половину параллельно; со своей строкой в
`reference.md`, иначе `apps/ui/src/animations/docs.test.ts` не пропустит.

**Ответ владельца.** Похоже, что сделанный под это модуль не раскатили полноценно как пример — надо
исправить.

**Статус.** `открыто` — все четыре копии на месте, работают и покрыты своими тестами; шаг собрать и
раскатить, чтобы копии стали вызовами. Кит, наша сторона.

### Строка-подсказка под центром стола написана дважды

**Что не хватает.** «Строка о том, чего стол ждёт» — всегда смонтированная плашка под центром,
которая проявляется и гаснет переходом (`opacity` + сдвиг 132px → 146px за 260ms `--ease-out`), —
написана отдельным CSS два раза: `DefenseReleaseStory.module.css`'s `.ask` (одобренный источник) и
`_Board.module.css`'s `.ask` (#101, Fix B — борд получил её вместе с отменой панели на `defend`).
Значения совпадают до пикселя, потому что вторая копия процитирована с первой; ничто, кроме
внимания, их в этом состоянии не держит.

Это переход CSS, а не `play()`, поэтому словарь `apps/ui/src/animations/` его и не покрывал: там
живут полёты по координатам, а не состояния смонтированного элемента. Модуля под «поверхность,
которая проявляется на месте» в проекте нет ни в каком виде.

**Чем грозит.** Ровно то же, о чём предупреждает правило анимаций, только на ступень мельче фолда
пары: правка тайминга, сдвига или кривой в сцене не долетит до борда — а борд как раз то место, где
эту подсказку читает игрок, и расхождение будет видно только тому, кто откроет обе страницы подряд.
Плюс `prefers-reduced-motion`: у копии борда он погашен (`transition: none` + конечная поза), у
копии сцены — нет; сегодня это разные требования (сцена — витрина), но и это различие держится
только на том, что кто-то о нём помнит.

**Унаследованное ограничение обеих копий: ширина строки упирается в половину стола.** `.ask` —
абсолютный flex-ряд, привязанный `inset-inline-start: 50%`, так что его shrink-to-fit считается от
50% до правого края контейнера, то есть по половине ширины стола; ни `max-inline-size`, ни правила
переноса нет. На узком экране ряд (плашка + кнопка отказа на борде) переносится или дотягивается до
колонки колод. Копия борда сознательно оставлена точной цитатой сцены, а не «улучшена»: правка
привязки — это правка раскладки, которую в этом проекте нечем проверить (борд с `defend`-пендингом
не поднять без двух реальных пиров, jsdom не считает геометрию), и она развела бы копии дальше как
раз там, где эта запись просит их свести. Чинить — вместе, в том же заходе, что и всё остальное
здесь: одно правило на обе, `inset-inline: 0` + центрирование вместо якоря в 50%, чтобы сдвиг по Y
остался единственным, что анимируется.

**Что закроет.** Либо класс-утилита рядом с токенами (одно имя, один тайминг, свой
`prefers-reduced-motion`), либо — если таких «поверхностей на месте» наберётся больше одной —
маленький шаг в `apps/ui`, со своей строкой в `reference.md`. Раньше кода стоит решение, чем это
считается: словарь `animations/` сегодня описан как «полёты», и поверхность на месте в него не
входит по определению.

**Ответ владельца.** Словарь полётов написан для КАРТ, а не для текстов. На узкие экраны игра сама
по себе не рассчитана, так что ширина строки — не предмет. При этом анимации текстовых блоков и
ситуации вроде «взять карту с руки оппонента», где есть текстовая часть, вроде бы заводились —
выглядит как неполная реализация или упущение, а не как отсутствующий раздел словаря.

**Статус.** `открыто` — не «расширять словарь», а найти, что из заведённого под текстовую часть уже
есть, и почему строка написана мимо него. Ширину не трогаем.

### Граница матча у жестов и у очереди тактов заведена на ключ, который не меняется

**Что не хватает.** `<Board>` не перемонтируется на реванш — `_layout.tsx` не даёт ему `key`, один
инстанс обслуживает все матчи сессии, — поэтому и `useBeats`, и оба жестовых хука
(`_useBoardStaging.ts`, `_useDefenseStaging.tsx`) держат сброс «на границе матча»: карта, стоящая в
центре, оплаченная цена, вооружённая стрелка, флаер в полёте, припаркованный `useHandArrival` — всё
это принадлежит матчу, а не компоненту. Сбросы написаны и покрыты тестами.

Ключ, на котором они висят, матч не различает — **на этой ветке, по состоянию на 2026-08-20**. В хук
приходит `intro.gameId` → `session.gameId` → `useLobby.ts`'s `startGame`: `const id =
current.hostId`, то есть **peer id хоста** («the board route is keyed by the host peer id» — там же).
Для всех матчей одной комнаты это одно и то же значение, поэтому второй `startGame` даёт тот же ключ
и `useLayoutEffect` не срабатывает ни разу. Найдено на ревью #101 (Fix C) и записано в Fix D: сам
сброс верный, инертна граница.

> ⚠️ **Это свойство ветки, а не закон.** Параллельно идёт работа над реваншем на месте (#19), где
> у каждого матча появляется собственный id, — то есть посылка «`gameId` = peer id хоста, один на
> все матчи» там уже неверна и перестанет быть верной здесь в момент слияния. Соответственно
> меняется и вывод: сброс из инертного становится живым, и это не «починка этой записи», а её
> естественное закрытие с другой стороны. **Перед тем как опереться на любое из двух утверждений —
> перечитать `startGame` и то, что доезжает до `intro`.** Ветка #19 переименовывает и
> `pages/board/[gameId]/_layout.tsx` → `index.tsx`, так что путь в этой записи тоже устареет.

**Чем грозит.** Сегодня — ничем: реванша «на месте» в приложении нет, единственный вход в матч
проходит через лобби и перемонтирует борд, так что состояние умирает вместе с инстансом. Дыра
латентная и станет живой ровно в тот день, когда появится «играть ещё раз» без ухода с маршрута
борда: реванш, начатый посреди шага оплаты релиза, оставит оплаченную карту лежать на НОВОМ столе
навсегда (`_Board.tsx` рисует `paidCost` без гварда), первый такт нового матча позовёт
`clearPaidCost`/`takeStagedRelease` по состоянию закончившегося, а `StageState`, оставшийся в
`standing`, нарисует первый релиз нового матча в слоте сцены поверх пары. Хуже обычного класса
«забыли сбросить» тем, что сброс есть и выглядит рабочим — на код-ревью он читается как закрытый
вопрос.

**Что закроет.** Идентификатор, который чеканится там, где начинается матч, и доезжает до всех
пиров одинаково: счётчик матчей или уже существующий `seed` дилера в `startGame`, положенный в
payload `GAME_STARTING` рядом с `gameId` (маршрут при этом не трогается — `gameId` остаётся peer id
хоста, иначе поедет URL борда), и проброшенный через `_layout.tsx`'s `intro` в оба жестовых хука
**и в `useBeats`** — у очереди тактов ровно та же граница и ровно тот же инертный ключ, чинить их
порознь бессмысленно. Проверяется это только двумя реальными пирами: в jsdom «реванш» — это смена
пропа, которую тесты уже делают напрямую, и она ничего не говорит о том, меняется ли проп в
приложении.

**Ответ владельца.** Новый матч — это НОВЫЙ МАТЧ: весь путь начинается заново, с экрана лобби.
Кнопки реванша и подобного у нас нет и не планировалось. Функция выдумана, и вопрос «как её сделать,
чтобы не обосраться» — следствие того, что её выдумали.

**Статус.** `закрыто` — реванша на месте нет в игре. Единственный вход в матч идёт через лобби и
перемонтирует стол, так что состояние умирает вместе с инстансом, и граница матча внутри компонента
не нужна.

### Превью веера снова закрывает центр во время оплаты, и нажатие сквозь него отменяет релиз

**Что не хватает.** #100 увёл веер в `pointer-events: none`, пока пара стоит сложенной в центре, — по
одной причине: ховер-превью `Hand` поднимается ровно в то место, где стоит пара, и закрывает её
(своя причина ComboStory). #101 (Fix C) заставил этот гвард УСТУПАТЬ, когда открыт шаг оплаты
релиза: веер — единственный, кто может эту цену назвать (панель для `discardForRelease` снята, а
клавиатурной двери у веера нет), так что инертный веер делал цену неоплачиваемой никаким вводом.
Уступка верна — дедлок хуже перекрытия, — но вторая половина сделки нигде не была записана.

А половина такая: на всю фазу оплаты превью снова встаёт над стоящей парой. И у `Hand`'s `.zoom`
стоит `pointer-events: none`, поэтому нажатие, попавшее в превью, ПРОВАЛИВАЕТСЯ на то, что под ним,
не находит ни одного `[data-hand-slot]` — и слушатель `mousedown` на корне стола
(`_Board.tsx`, «промах = передумал») читает его как промах и отменяет релиз. То есть попытка ПРОЧИТАТЬ
карту может отменить розыгрыш.

**Чем грозит.** Тихая потеря хода в шаге, где игрок как раз изучает руку: он навёл карту, чтобы
прочитать, нажал — и релиз уехал домой. Отмена сама по себе безопасна (`cancelRelease` ничего не
эмитит, проекция вернёт карту), но игрок не связывает её со своим движением: он нажал «на карту».
Плюс это тот же класс, что и две записи выше: тесты зелёные, потому что jsdom не считает
`pointer-events` и `fireEvent` попадает туда, куда его послали.

**Что закроет.** Решение о том, чем превью является для указателя. Исключить его из «промаха»
нельзя, пока оно не цель события; сделать его целью (`pointer-events: auto`) — значит отдать ему
ховер, который его же и поднял, и получить мигание. Варианты, оба в `Hand`: превью, которое
поднимается НЕ над центром, когда сцена говорит, что центр занят (проп «где нельзя»), либо превью,
которое остаётся прозрачным для указателя, но ставит на время своего показа флаг, читаемый
слушателем промаха. Первое честнее: перекрытие тогда просто не возникает, и гварду не нужно было бы
ни появляться, ни уступать.

**Ответ владельца.** Случая не существует: превью вызывается наведением мыши, поэтому нельзя
одновременно держать превью и ткнуть в область, которую оно закрывает. Уходит превью сразу, как
только мышь ушла, — это уже предусмотрено. Запись описывает выдуманное событие.

**Статус.** `закрыто` — не воспроизводится по природе жеста.

### Такт летит из bounding-box наклонённого слота веера — это I6, только наоборот

**Что не хватает.** `comboBeat.tsx`'s `foldIn` и `defenseBeat.tsx`'s `runCovered` берут исходный
прямоугольник своей карты как `rectOf(anchors.handSlotAt(i))` — сырой `getBoundingClientRect()`
слота руки. Слот повёрнут (`slotPlacement` даёт каждому свой угол), поэтому его bounding rect — это
коробка ВОКРУГ наклонённой карты: шире и выше самой карты. Ровно то, от чего предостерегает **I6**, и
ровно то, что соседняя нога той же цепочки уже делает правильно: `anchors.seatBox` кладёт
`cardBoxIn(rect, CARD_W)`, а `useHandArrival.boxOf` — тоже, и со своим комментарием почему («центр
карты чистый поворот оставляет на месте»). Две ноги одной цепочки меряют коробку по-разному.

**Чем грозит.** Первый кадр полёта стартует не с той коробки: карта чуть больше и чуть смещена
относительно того места, где она на самом деле лежала, — тем заметнее, чем сильнее отклонён слот, то
есть на краях веера. Это не ломает такт, поэтому и живёт: тесты меряют нулевые прямоугольники jsdom,
где разница между двумя способами равна нулю по определению.

**Что закроет — и почему это не однострочник.** Кандидатов два, и они дают РАЗНЫЕ ответы:
1. `cardBoxIn(slotRect, CARD_W)` — то же, что делает `seatBox`. Дёшево, но привязано к DOM: считает
   от текущего прямоугольника, включая ховер-подъём слота под курсором.
2. `slotBox(i, total)` из `_useBoardStaging.ts` — честный путь: коробка выводится из
   `slotPlacement(i, total)`, DOM не нужен, ховер не влияет. Но нужен `total` — длина ВЫРИСОВАННОГО
   веера, а не руки. У такта её нет: он работает против `base`, а веер в этот момент отфильтрован
   тем, что стоит на столе. Fix D round 2 закрыл ровно эту рассинхронизацию на другом шве (клик в
   веере) — и она же делает наивный `you.hand.length` здесь неверным. Плюс `slotBox` живёт в хуке
   страницы, а такты — в `features/`, так что переезжать ему в `anchors` (слой `entities`), где уже
   лежат `seatBox`/`handSlotAt`.

Выбор между 1 и 2 — решение о геометрии, которое в этом проекте нечем проверить: борд с чужим
пендингом не поднять без двух реальных пиров, а jsdom обе ветки не различает. Поэтому записано, а не
поправлено на глаз: неверный выбор ухудшит КАЖДЫЙ локальный фолд, а сегодняшняя ошибка ограничена
парой пикселей на краю веера.

**И вторая половина той же строки: индекс берётся из `base`, а слот ищется в живом DOM.**
`handIndex` считается по `ctx.base.you.hand`, а `a.handSlotAt(handIndex)` спрашивает
`hand.current.querySelectorAll('[data-hand-slot]')[index]` — веер, отрисованный ПРЯМО СЕЙЧАС. Это тот
же класс, что off-by-one, закрытый в Fix D round 2 на клик-шве: индекс одного массива, применённый к
другому. После Fix D round 3 нога стреляет только на реджойне, где ничего не застейджено и веер
показывает ту же руку, что и `base`, — так что расхождение стало гораздо менее вероятным, но
структурно оно на месте: ничто не удерживает два массива в согласии, кроме этого рассуждения.

**Ответ владельца.** Карта с края веера не может стартовать наклонённой: упущен момент, что под
ховером она ВЫРАВНИВАЕТСЯ. Вопрос проебал часть движения и спрашивает про то, чего в ней нет.
Проблемы выдумывать не надо.

**Статус.** `открыто` — предсуществующее; тест `defenseBeat.test.tsx`'s «flies our own defence out of
the fan slot it left» (#101, Fix D) заглушает этот прямоугольник напрямую и в своём комментарии
говорит, что это сырой bounding rect и эта самая запись, а не одобрение способа. Строка в реестре
находок на странице аудита (`apps/playground/stories/AnimationAuditStory`) заведена, статус `open`.

### Клик в веере может начать вторую игру, пока первая ещё в пути

**Что не хватает.** `onHandPlay` отказывает любому ВЫТЯГИВАНИЮ, пока `stagedRef.current` занят, —
пара, стоящая в центре, неприкосновенна. У клика такого правила нет: `_useBoardStaging.ts`'s
`onCardClick` возвращает `false`, когда что-то стоит, но это не шаг «выбери партнёра» и не шаг
оплаты, и клик уходит дальше — в обычный клик-жест (`_useBoardInteractions.ts`), который разыграет
любую карту из `state.playable` без цели. А `state.playable` в этот момент ещё старый: проекция
отстаёт на круг, пендинг не приехал. То есть между отправкой первой игры и ответом рефери веер
принимает вторую.

Найдено в Fix D round 2 рядом с off-by-one на том же шве (клик отдавал индекс отрисованного веера в
массив всей руки, и с релизом на столе клик по запаснóй карте переигрывал сам релиз). Индекс
починен; это — вторая, независимая половина.

**Чем грозит.** Движок вторую игру почти наверняка отклонит (у него уже открыт пендинг), и карта
вернётся в веер через сторожа отказов — то есть тихо. Но игрок увидит, как карта, которую он ткнул
«между делом», улетает и прилетает обратно без объяснения, и это ровно тот класс, из-за которого
затевался весь аудит: стол делает то, о чём его не просили.

**Что закроет.** Одна строка: `onCardClick` должен ГЛОТАТЬ клик, а не пропускать его дальше, пока
`stagedRef.current` занят (`return true` вместо `return false` в ветке
`s.phase !== 'partner' || !s.support`) — то же правило, что у `onHandPlay`, и согласуется с тем, что
`stateAt` в этот момент не подсвечивает в веере ничего: шага, который веер отвечает, нет. Не сделано
в Fix D: это смена поведения на пути, общем для вытягивания и клика (не только для того окна,
которое открыл этот заход), а проверить её на живом столе нечем.

**Ответ владельца.** Розыгрыша кликом в игре нет — играют перетаскиванием. По описанию не
воспроизводится, в том числе в плейграунде: там ховер руки и без того блокируется, пока карта
стоит в центре. Похоже на неполноту переноса на борд, а не на игровую ситуацию.

**Статус.** `открыто` — вопрос к переносу на борд, а не к жесту: клика как способа сыграть нет.

### Два `useFlyer` на одной странице сталкиваются ключами React

**Что не хватает.** `useFlyer` нумерует свои узлы собственным счётчиком (`seq`, React-ключ узла в
`overlay`, инвариант I5 — свежий узел на каждый полёт). Счётчик приватный и у каждого экземпляра
начинается с нуля, а `overlay` — это массив, который сцена склеивает с чужими
(`[...flyer.overlay, ...arrival.overlay]`). У `useHandArrival` ключи строковые (uid), поэтому с ним
столкновения нет; у двух `useFlyer` первый же поднятый носитель в обоих будет `key={1}`, и React
получит двух детей с одним ключом. Правила «сколько носителей на странице» не решено вовсе: ничего
ни в модуле, ни в доках не говорит, можно ли поднять второй.

Найдено при выделении `_useCoverFlight.ts` (#102, задача 3): двум слотам одного стола (прикрытие и
слот судо в `_useDefenseStaging.tsx`) нужны независимые гейты `landed`, то есть два экземпляра
модуля, а модуль по своему интерфейсу владеет носителем.

**Чем грозит.** Не гипотетика: на защите окно достижимо — судо ещё летит к своему слоту, игрок
кликает партнёра, и фолд поднимает `fold`, пока `sudo` в воздухе. React в этот момент переиспользует
узел не того носителя: карта может мигнуть чужой рубашкой или потерять свои inline-координаты (I10),
и это ровно тот класс отказа, ради которого I5 и заведён.

**Что закроет.** Ключ должен быть уникален по экземпляру, а не по счётчику: `useFlyer` заводит свой
идентификатор (`useId()`) и отдаёт `key={`${id}-${h.seq}`}`. Тогда сцена может поднять сколько угодно
носителей, а вопрос «можно ли второй» перестаёт существовать. Не сделано в задаче 3 — это правка
`@release/ui/animations`, а задача была чистым рефакторингом фронтенда с запретом на смену поведения.

**Статус.** `открыто`; обойдено локально и в открытую: `useCoverFlight(shared?)` принимает уже
существующий носитель, и `_useDefenseStaging.tsx` отдаёт обоим экземплярам ОДИН свой `useFlyer` —
поэтому на живом борде столкновения сейчас нет. Строка в реестре находок на странице аудита
(`apps/playground/stories/AnimationAuditStory`) заведена, статус `open`.

### У ответа Monitoring на 503 нет движения вообще

**Что не хватает.** Три метода отвечают на Error 503, и у двух из них жест — это полёт: Debugger
вытаскивается из веера, релиз вытаскивается из своей зоны, оба летят в слот прикрытия
(`playToCenter`, поза `COVER_POSE`). У Monitoring движения нет ни одного: карта отвечает оттуда, где
стоит, и там же остаётся. Утверждённый источник (`Error503Story` в плейграунде) жеста под него не
содержит вовсе — история выстреливала Monitoring сама, поэтому и вопроса не возникло.

**Чем грозит.** Отправить его в центр и вернуть — соврать про то, что произошло: карта не уходила из
зоны и не сгорала. Поэтому в задаче 9 (#102) сделано минимальное честное: нажатие на слот шлёт
`RESOLVE` и не двигает ничего, а тревогу уносит такт (`defenseBeat.runNeutralized`, который для
Monitoring и сам ничего не везёт). Ценой того, что единственный ответ из трёх остаётся без
подтверждения на месте: подсветка слота гаснет, и всё — между нажатием и тактом на экране не
происходит ничего.

**Что закроет.** Движение, которое говорит «эта карта СРАБОТАЛА, не уходя»: вспышка/пульс на самом
слоте (модуля под это в словаре нет — `play('...')` пресета «карта отработала на месте» не
существует). Оформляется пресетом в `apps/ui/src/animations/`, строкой в `reference.md` и рецептом в
`recipes.md`; жест при этом не меняется — меняется только то, что видно после него.

**Статус.** `открыто`. Строка в реестре находок на странице аудита
(`apps/playground/stories/AnimationAuditStory`) заведена, статус `open`.

### ЗАКРЫТО: ответ движка обгонял свою же защиту в полёте, и карта на такт возвращалась в веер

**Что было.** `_useDefenseStaging.commitAndFly` шлёт `onResolve` СИНХРОННО и только потом поднимает
носитель и везёт карту веер→слот прикрытия. Ответ движка (`covered`) приезжает ВНУТРИ этого полёта:
у хоста движок локальный и отвечает в том же тике, у клиента достаточно круга короче одного полёта.
Тот коммит рендерился, когда `beats.shadow` ещё `null`, то есть `state === live`: пендинга нет,
`answering` — ложь, карты в руке нет, и пассивный эффект-догонялка чистил `staged`, читая это как
«розыгрыш принят, работа жеста окончена». Следующий коммит рисовал тень (`base`), где карта снова в
руке, а `staged` уже пуст — карта возвращалась в веер и лежала там весь такт, рядом с собой же в
центре. Второй артефакт того же коммита — такт принимал пустой `el` за реджойн и вёз карту из веера
второй раз — закрыт в Fix D round 3.

**Чем закрыто (Fix D round 4).** Обе половины сразу, потому что порознь каждая делала хуже — это
было записано здесь как открытый вопрос и подтвердилось на реализации:

1. **Догонялка не верит проекции, пока её собственный носитель не отпустил карту.** Гейт — `landed`,
   а не «идёт ли такт»: оба пути розыгрыша (простая защита и фолд с судо) ставят его в `false` при
   отправке и в `true`, когда носитель отпустил карту, так что вопрос задаётся ровно тому, кто на
   него отвечает, и никакой новой связи между `useBeats` и хуком не понадобилось. `landed` в
   зависимостях эффекта, поэтому в момент приземления он перезапускается и чистит, если беат так и
   не пришёл. **И каждый выход обоих полётов теперь отчитывается `setLanded(true)` из `finally`** —
   отменённая анимация (реджект `.finished`) иначе оставила бы `landed` ложным при
   `phase: 'dispatched'`, то есть дырку в веере навсегда, что хуже призрака, ради которого всё
   затевалось.
2. **Такт вообще не трогает свою защиту:** гвард стал `!(mine && handoff)` вместо
   `!(mine && handoff?.el)`. Раз staged доживает до `release()` такта, жест сам довозит карту и
   держит её (носитель → статичный рендер в ОДНОМ коммите: `flyer.drop` и `setLanded(true)`
   батчатся), так что слот занят непрерывно. Ленивое чтение `el`, которое обсуждалось здесь как
   вторая половина, оказалось не нужно и было бы хуже: оно вскрыло бы гонку, ради которой снимок
   хендоффа и существует (см. комментарий `comboBeat.runAttack`). Спрашивать «был ли розыгрыш
   застейджен» вместо «успел ли появиться его узел» убирает тайминг из вопроса совсем.

Заодно `_Board.tsx` спрашивает про застейдженную защиту ДО `answering`, а не внутри: на транзитном
коммите `answering` мигает в ложь, и ключ на нём обнулял хендофф со стороны хода — такт, запланированный
следующим коммитом, принял бы свою защиту за реджойн.

**Чем это пиналось.** `comboHandoff.test.tsx` — тот самый коммит, которого не строил ни один
фикстур: реальные `useBeats` + `useDefenseStaging` + `useDefenseBeat`, в порядке объявления
`_Board.tsx` (сначала `useBeats` — этот порядок и есть гонка), с полётом жеста, удержанным в воздухе.
Обе половины проверены мутацией порознь.

**Статус.** `закрыто`. Строка в реестре находок на странице аудита
(`apps/playground/stories/AnimationAuditStory`) переведена в `ok`.

---

### ЗАКРЫТО: Событийная карта, ушедшая домой, объявляется `discarded`

**Что не хватает.** `packages/engine/src/fake/core.ts`'s `bankToDiscard` при банковке проверяет
`c.event`: карта с этим полем уходит не в общий сброс, а обратно в колоду событий
(`state.decks.events`, строки 156–169). Но `discarded`, которым эта банковка отчитывается перед
столом, называет её пунктом назначения сброс всегда — поля, которое сказало бы «на самом деле она
поехала домой», у события нет вовсе. А сама карта на столе несёт **обычный** `release-<slot>` id, а
не `ai-release-*` — так задумано, чтобы стоящий `ai-release` в зоне читался как рядовой релиз.
Вместе это значит, что у доски нет НИ ОДНОГО способа отличить один случай от другого: ни по событию
(оно молчит), ни по карте (она замаскирована).

**Чем грозит.** Не гипотетика для этой задачи: `neutralized`-план сацерифайса (`planBeats.ts`)
утверждает один такой сброс как обычный — сожжённый `ai-release`, отданный в жертву 503, у
`defenseBeat.runNeutralized` летит в кучу сброса тем же движением, что и рядовая карта, и там
никогда не приземляется по-настоящему (колода событий его уже забрала). Общее и не новое:
`discardBeat.tsx`'s `sourceOf`/планировщик несёт тот же слепой пробел для любого события —
Crush, обычное уничтожение, что угодно, банкующее событийную карту через `bankToDiscard`.

**Что закроет.** Пункт назначения на `discarded` (например, `destination: 'discard' | 'events'`),
который читает планировщик вместо того, чтобы предполагать сброс всегда; либо отдельное событие для
пути «событийная карта вернулась в свою колоду», которое `discardBeat`/`defenseBeat` умели бы
развести от рядового сброса без гадания по id.

**Чем закрыто.** Не правкой `discarded` — заведённым рядом фактом, который читает сам, минуя гадание
по id. `packages/engine/src/view.ts`'s `ReleasedView` несёт `event?: CardId` — тот самый
events-deck id, которым карта уйдёт домой, если он у неё есть (#106); `toBoardState.ts` кладёт его в
`you.releaseEvent`/`opponents[].releaseEvent`, и `planBeats.ts` читает его через `releaseEventsOf`
везде, где раньше пришлось бы гадать по `discarded`, — в том числе для `AiTail`'s `crush`
(`aiTailAfter`, `destination: 'events' | 'discard'`). Второй половиной было не только объявить
факт, а исправить полёт, который молчание уже сломало: сожжённый в жертву 503 `ai-release`
(`neutralized`-план сацерифайса) у `defenseBeat.tsx`'s `runNeutralized` летел в кучу сброса тем же
движением, что рядовая карта, и там никогда по-настоящему не приземлялся — колода событий его уже
забрала. `runNeutralized` теперь читает `releaseEventsOf` (ту же проекцию, что и план) и посылает
такую карту `returnToDeck`, а не в кучу.

**Статус.** `закрыто`. Строка в реестре находок на странице аудита
(`apps/playground/stories/AnimationAuditStory`) переведена в `ok`.

---

### Пендинг без дедлайна останавливает партию

**Что не хватает.** `apps/frontend/src/network/session/referee.ts:402` истекает по времени только
`defend`-пендинги; `:422` приостанавливает часы хода, пока открыт ЛЮБОЙ пендинг. У `neutralize503`,
как и у `handLimit`, `pickFromDiscard`, `requestCard`, `giveCard` и `crush`, своего дедлайна нет
вовсе. `discardForRelease` сюда не относится: `:421–427` держит его исключением из той же
приостановки именно потому, что часы хода — свои для владельца хода, и не могут стоять, пока он
платит цену собственного релиза (`stampTurnClock`, `packages/engine/src/fake/reduce.ts`); по
истечении `turn.deadline` этот пендинг форс-резолвится сам (`cancelRelease`, `:461–470`), а не
ждёт закрытия извне.

**Чем грозит.** Подключённый игрок, который не отвечает на `neutralize503` (или на любой из
остальных пяти видов), замораживает партию для всех — часы хода стоят, пока пендинг открыт, а
закрыть его извне некому. Найдено при сборке #102: сцена сметания у `discardBeat.tsx` рассчитана на
то, что пендинг рано или поздно закроется, но тайм-аута, которым это гарантировано, не существует.

**Что закроет.** Дедлайн на каждый из шести видов пендинга разом — правка, общая для всех, а не для
одного `neutralize503`. Вопрос заведён отдельным issue (issue drafted, not yet filed) — правка не
принадлежит одной задаче.

**Статус.** `открыто`. Строка в реестре находок на странице аудита
(`apps/playground/stories/AnimationAuditStory`) заведена, статус `open`.

---

### Клипы вылета едут в продуктовый билд без подтверждённых прав

**Что не хватает.** Четыре `.mp4` (`freshleb-whistlindiesel.mp4`, `gato-truco-gato.mp4`,
`IHa0T7Ffr43z1kTd.mp4`, `doc_2026-07-31_23-09-35.mp4`) переехали из папки истории
(`apps/playground/stories/interactive/eliminate/`) в
`apps/frontend/src/features/board-beats/eliminate/` и теперь собираются в билд фронта (#103). Ни у
одного из четырёх нет ни автора, ни лицензии, ни источника — в песочнице плейграунда это ничем не
грозило, в продукте это раздача чужого материала.

**Чем грозит.** Релиз с чужим видео внутри. Плюс вес: 1.9 МБ на диске сборки — не в JS-бандле
(Vite кладёт каждый клип отдельным хешированным ассетом, и до монтирования оверлея ни один не
запрашивается), но раздаётся это оттуда же, откуда и всё остальное, без CDN.

**Что закроет.** Решение по каждому клипу: подтверждённая лицензия либо замена своим материалом.
Такта это не касается ни строкой — `ELIMINATION_CLIPS` собирается глобом по папке, файлы меняются
как файлы. #103 везёт их как есть намеренно: без клипов такт нечем ни показать, ни проверить.

**Статус.** `открыто`. Строка в реестре находок на странице аудита
(`apps/playground/stories/AnimationAuditStory`) заведена, статус `open`.

### Правила ставят Security Bug в центр на время выбора — движок к этому моменту уже сбанчил её

**Что не хватает.** `docs/rules/cards.md:126`: атакующая карта лежит в центре стола всё время, пока
атакующий выбирает, — это и есть демонстрация столу, что её разыграли. `onHandDefend`
(`packages/engine/src/fake/attacks.ts:191`) отправляет её в сброс через
`bankSpent(..., 'attackSpent')` в ТОЙ ЖЕ редукции, которая поднимает `pending: requestCard` (там же,
:203). То есть к моменту, когда атакующий что-то выбирает, проекция уже держит карту атаки в куче
сброса.

**Чем грозит.** Борд не может поставить в центр карту, про которую проекция говорит «она в сбросе»:
поставить — значит завести второй источник правды о том, где карта, и разойтись с кучей на первом же
разборе. Поэтому на время `requestCard` не-атакующие получают строку в доке и пустой центр —
публичное по правилам действие приходит к столу без своей улики, ровно той, ради которой правило и
написано («это и есть демонстрация, что её разыграли»). Расхождение правил с движком, а не
визуальная недоделка: тактом его не закрыть, и любая попытка закрыть его на борде будет выдумкой про
местоположение карты.

**Что закроет.** Движок паркует карту атаки на самом пендинге `requestCard` — ровно так, как `defend`
уже паркует брошенную атаку, — и снимает её в сброс тем же переходом, который закрывает пендинг.
Тогда центр стоит из проекции, а не из такта, и показывать специально нечего. Это изменение поведения
движка со своей conformance-поверхностью, поэтому оно записано, а не сделано в #105.

**Статус.** `открыто`. Строка в реестре находок на странице аудита
(`apps/playground/stories/AnimationAuditStory`) заведена, статус `open`.

### Сторонний игрок не видит передачи карты вообще — событие до него не доезжает

**Что не хватает.** `handTransfer` логируется с `visibleTo: [from, to]` в обеих точках, где он
вообще возникает (`packages/engine/src/fake/handAttacks.ts:61` — случайная кража, `:230` — отдача по
запросу), а `forViewer` (`apps/frontend/src/network/session/audience.ts:11`) выбрасывает событие
ЦЕЛИКОМ у всех, кого нет в списке. `redactFor` (`packages/engine/src/redact.ts`) умеет ровно
обратное — вырезать секретное ПОЛЕ, оставив событие на месте, — но знает только про `drawn`. Значит
нога `watcher` в `transferBeat.tsx` в проде недостижима: сторонний игрок не получает ничего, и у двух
соперников просто меняются счётчики руки.

**Чем грозит.** Это тот же самый отказ, который описан в шапке самого `redact.ts` про добор: «добор
соперника доезжал до остальных пиров ничем — событие анимировать нечего, только счётчик руки
тикнул». Он вернулся, потому что починку не обобщили: `drawn` вылечили поимённо, а `handTransfer`
завели заново с той же ошибкой. Пока так, стол молча теряет факт, публичный по правилам для Security
Bug (`cards.md:127` — «стол видит открыто ... открытую передачу»), а нога, написанная и покрытая
тестом, не играет ни разу — то есть гниёт без единого сигнала.

**Что закроет.** Снять `visibleTo` с обеих точек `handTransfer` и расширить `redactFor` так, чтобы
она вырезала `card` у не-участников, — ровно как она уже делает для `drawn`. Это строго больше
информации о ТОМ, ЧТО передача была, и ноль информации о ТОМ, ЧТО именно переехало. Нога `watcher`
при этом не меняется ни строкой: она для этого дня и написана.

**Статус.** `открыто`. Строка в реестре находок на странице аудита
(`apps/playground/stories/AnimationAuditStory`) заведена, статус `open`.

### Правила не говорят, узнаёт ли стол, какую карту забрала случайная кража

**Что не хватает.** `docs/rules/cards.md:112` (Bug / Out of Memory / Legacy Code) говорит только:
«Взять **одну случайную** карту из руки соперника», — и ни слова о том, видит ли стол, какая карта
переехала. Для Security Bug тот же файл это решает прямо и подробно (`cards.md:127`: «стол видит
открыто, какую карту запросили, и видит открытую передачу»); для случайной кражи — молчит.

**Чем грозит.** Разница между двумя прочтениями видна на экране и ничем больше не проверяется. Борд
сегодня отдаёт `card` только двум участникам (`visibleTo: [from, to]`), и сторонний игрок в обоих
случаях не узнаёт ничего, — но это следствие устройства события, а не решение по правилам, и оно
совпадает с одним из прочтений случайно. Догадаться «по смыслу» тут особенно легко и особенно
опасно: под догадку уже написан такт, она закрепится тестом и станет источником, по которому сверятся
следующие. Обратная сторона тоже стоит денег: если стол ДОЛЖЕН видеть, какую карту забрали случайно,
то нога `watcher` обязана перестать быть закрытой, а это другой такт, а не параметр.

**Что закроет.** Ответ владельца правил одной строкой в `cards.md` — «стол видит / не видит, какая
карта ушла при случайной краже», — на месте абзаца про эффект на руку. Здесь ответ не выдумывается:
проект прямо запрещает дорешивать правила по смыслу.

**Статус.** `открыто` — вопрос к владельцу правил. Заведён как открытый вопрос в
[`docs/rules/backlog.md`](../rules/backlog.md), где ему и место: этот файл держит анимационную
сторону. Метка `> ❓ **Не из правил.**` в самом `cards.md` НЕ поставлена — вставка сдвинула бы **18**
рабочих указателей вида `cards.md:NNN` (из **40** совпадений объявленного grep; остальные — проза
самих находок про эту проблему, растущая от того, что её обсуждают), и все они указывают на 112 или
ниже. Это записано там же отдельным долгом, с разбивкой, названной ценой и починкой. Строка в реестре находок на странице аудита
(`apps/playground/stories/AnimationAuditStory`) заведена, статус `open`.

### Рецепт, разошедшийся со своей сценой, не роняет ничего

**Что не хватает.** `apps/ui/src/animations/docs.test.ts` роняет сборку, если у пресета нет строки в
`reference.md`; `apps/playground/stories/docs.test.ts` роняет её, если у модуля со страницы аудита
нет упоминания в `reference.md`, если у анимированной сцены нет «живой ссылки» в `recipes.md` и если
`board:`-путь указывает на несуществующий файл. Ни одна из проверок не смотрит, ОПИСЫВАЕТ ли рецепт
ту сцену, которую сам назвал живой ссылкой. Наличие ссылки проверяется, соответствие — нет.

**Чем грозит.** Уже стоило двух рецептов, и оба чинились в #105. «Opponent takes your card» описывал
двухшаговый перелёт в центр ЧУЖОГО ВЕЕРА с `rotate(180)` и падением `zIndex` до 30, чтобы подоткнуться
под веер; сцена с коммита `440bc56` — того самого, которым заведена `docs/animations/`, — рисует
соперников `Seat`'ами и целится в `cardBoxIn(seat, r.width * SEAT_SHRINK)`. «Taking an opponent's
card» описывал центрированный грид из `gridPositions`, `ORIGIN`, `COLS_MAX`, `DEAL_CARD_W` и
`slotRefs`, а `PickOpponentCardStory` рисует `<Hand faceDown>`, съезжающий сверху; по всему репозиторию
из этих пяти имён в коде выжило одно — `slotRefs`, и то в `GameEndStory`/`ComboStory` про совсем другое;
грида, который рецепт якобы транскрибирует, не было никогда. Тем же заходом нашлись и две его
метастазы, обе тоже починены: строка `DEAL_CARD_W | 150 | PickOpponentCardStory` в `glossary.md`
называла константу, которой нет нигде (и рядом с ней `COLS_MAX`, `GAP_X`/`GAP_Y`, `ORIGIN` — та же
мёртвая геометрия), а шаг 2 рецепта «Take a specific card» не знал про `confirmWanted` и
`ConfirmAction`, хотя страница аудита про них уже писала — то есть две доковые поверхности расходились
уже друг с другом. Все три сцены и глоссарий приведены в порядок в #105; НЕ починена дыра, которая дала
им сгнить. Опаснее обычной дыры тем, что доки читают ВМЕСТО кода: следующая задача строит по описанию,
и расхождение вылезает уже реализованным.

**Что закроет.** Проверка, которая берёт у каждого рецепта его «живую ссылку», читает названный файл
сцены и требует, чтобы имена, которые рецепт пишет обратными кавычками в «Elements / refs» и
«Sequence», в этом файле встречались. Ложные срабатывания на именах из шагов и пресетов снимаются
списком известных (`useHandArrival`, `play`, `flipCard` и т.п.) — но `fanRef` против `seatRefs` и
`gridPositions` против отсутствия таковых она ловит сразу, а это ровно то, что пропущено дважды.

**Статус.** `открыто`. Строка в реестре находок на странице аудита
(`apps/playground/stories/AnimationAuditStory`) заведена, статус `open`.

---

### Триггер AI сбанчен раньше, чем разрешится его собственный эффект

**Что не хватает.** Правила отпускают триггер и эффект одним движением — оба стоят и уходят вместе
(сценарный `resolveGeneric` у `AiCardsStory`, и собственный текст Bad Vibe в #106 говорит буквально
«всё уходит разом: триггер в сброс, AI-карта в свою колоду»). Движок этого не делает: `fireTrigger`
(`packages/engine/src/fake/triggers.ts`, ветка `trigger-ai`) заносит `discarded(trigger-ai)` сразу
следом за `aiRevealed`, ДО того, как `resolveAiEvent` вообще запускается, — а именно
`resolveAiEvent` решает, стоит ли AI-карта дальше и не открыт ли ею пендинг. К моменту, когда прогон
дошёл до вопроса «выдан ли прогноз», проекция уже держит триггер в куче сброса.

**Чем грозит.** Пока эффект уходит домой в том же батче, разница не видна — обе карты давно в своих
кучах к тому времени, как рисуется такт. Но стоит батчу оставить прогноз висящим — а именно так
устроены четыре из шести исходов (`crush`, `neutralize503`-мимик, `handLimit`, `pickFromDiscard`) —
и такт (`aiBeat.tsx`'s `run`) вынужден вести себя нечестно с одной из двух карт: эффект может стоять
и объяснять прогноз (`docs/animations/recipes.md`'s `standing`-ветка), а триггер — уже в куче, и
доске нечем удержать его рядом, не соврав проекции. Ровно этот компромисс сейчас и записан в новом
рецепте борда как «не баг, а асимметрия» — но асимметрия эта чужая, движковая, а не решение такта.

**Что закроет.** Перенос банковки триггера в движке — из `fireTrigger`'s ветки `trigger-ai` в конец
`resolveAiEvent`, после того как эффект уже решил, остаётся ли он висящим прогнозом или уходит
следом. Тогда обе карты банкуются в один момент, и рецепту не придётся объяснять, почему триггер и
эффект ведут себя по-разному. Форма ровно та же, что у уже записанной находки «Правила ставят
Security Bug в центр на время выбора — движок к этому моменту уже сбанчил её» (выше, «Правила
ставят Security Bug…») — движок банкует карту в той же редукции, которая поднимает пендинг, до того
как стол успевает её показать, — и стоит читаться рядом с ней, а не отдельным случаем.

**Статус.** `открыто`. Строка в реестре находок на странице аудита
(`apps/playground/stories/AnimationAuditStory`) заведена, статус `open`.

---

### Четыре старых слота центра на борде дублируют `centre.ts`

**Что не хватает.** `apps/frontend/src/pages/board/[gameId]/_Board.module.css:74-102` несёт
`-92px` (`.stageSlot`), `+92px` (`.costSlot`), `-180px` (`.sudoSlot`) и `42%`
(`.centre`/`.stageSlot`/…) как обычные CSS-литералы — те же числа, что `TableCentre/centre.ts`
называет единственным источником и отдаёт через `centrePlaceStyle`. Три новых места AI (#106) —
`cause`, `effect`, `picked` — уже читают его: `_Board.tsx` ставит им `style={centrePlaceStyle(set,
slot)}` вместо своих чисел. Четыре старых — нет, они старше самого файла-источника.

**Чем грозит.** Ровно то, ради чего `centre.ts` заведён (см. его собственную шапку): пустое место
легко описать дважды, и совпадают такие описания только вручную. Пока `centre.ts` и
`_Board.module.css` держат одни и те же четыре числа порознь, любая правка одного из них — это
правка, которую нужно не забыть повторить во втором, и ничто не роняет сборку, если её забыли.

**Что закроет.** Перевод четырёх слотов (`.stageSlot`, `.costSlot`, `.sudoSlot`, `.coverSlot`) на
`centrePlaceStyle('release' | 'defence', …)` — то же движение, каким в этой задаче уже пошли три
новых слота AI. Не сделано здесь: слоты уже отгружены и приняты (Defense Release, #101), а
регрессия в раскладке боя — которую нечем сразу поймать, координаты только на глаз — хуже, чем
записанное дублирование.

**Статус.** `открыто`. Строка в реестре находок на странице аудита
(`apps/playground/stories/AnimationAuditStory`) заведена, статус `open`.

---

### Крашу нечем показать вторую карту: релиз под своим Code Review

**Что не хватает.** Автоматическое разрушение слота — `destroySlot` без `reason`
(`packages/engine/src/fake/triggers.ts:88-92`) — выдаёт только `releaseDestroyed` и ни одного
`discarded`. А `toDiscardHeap` (`apps/frontend/src/entities/game/board/toBoardState.ts`) складывает
кучу по одной карте на `discarded`, так что собственной записи в куче нет ни у раздавленного
релиза, ни у подоткнутого под него Code Review.

Половина этого закрыта. Куча всё-таки показывает ОДНУ карту — свою заглушку верха сброса, и у
заглушки есть поза: `standInScatter(count)` (там же), ключ считается от ПОСЛЕбатчевого счётчика
сброса. Счётчик теперь приезжает в `planBeats` четвёртым аргументом — тем же способом и по той же
причине, что и `owed`: это факт о батче, о котором батч не может сообщить сам. Такт летит ровно на
эту позу (`AiTail.crush`'s `rest`), и обе стороны читают ОДНО значение, а не две копии формулы (I7).

Что осталось — вторая карта. Проверено на живом движке, а не выведено: `bankToDiscard` кладёт spoils
в порядке, в котором их перечисляет `destroySlot`, — `[релиз, Code Review]`. Поэтому при наличии
Code Review сверху оказывается именно он, раздавленный релиз лежит под ним, и в куче для релиза нет
НИЧЕГО: ни события, ни заглушки. Позы нет и у самого Code Review — заглушка одна, и на неё уже
некому смотреть дважды. Обе эти карты такт отпускает без разброса (свежий `jitter()`), потому что
придумать им место — значит завести второй источник для того, что рисует `toDiscardHeap`.

Отдельно стоит помнить: заглушка держится, только пока карта РЕАЛЬНО верхняя. Как только сверху
ляжет карта со своим `discarded`, свод снова кончается на настоящем верхе, заглушка исчезает, а
сбанченная в тишине карта уходит из кучи, хотя счётчик её ещё считает. Это записано на месте, в
`toBoardState.ts` (комментарий к ветке заглушки).

**Чем грозит.** В парном случае карта прилетает в кучу и на последнем кадре подпрыгивает в чужую
позу — ровно то, ради устранения чего разброс сделан единым источником (I7), — а релиз под Code
Review вообще исчезает под заглушкой. Сегодня это не видно ни на одном столе: ветка недостижима.
Краш открывает пендинг ВСЕГДА, потому что его целевой слот занят по условию (`resolveAiEvent`,
`triggers.ts:317`), а значит `sacrifice` всегда попадает в `neutralizeOptions`, и
`methods.length === 0` не наступает. То есть это не «баг, который ждёт игрока», а мёртвая ветка,
которая оживёт наполовину неправильной, если движок когда-нибудь заведёт крашу безответный путь
(или если `takeRelease` из `attacks.ts:99`, разрушающая релиз тем же способом, однажды попадёт в
такт).

**Что закроет.** Одна строка в движке: `reason` у автоматического `destroySlot`, чтобы он выдавал
`discarded` на каждую карту spoils, как это делает любая другая банковка. Тогда у обеих карт
появляется собственный event id, куча их складывает по-настоящему, и такт летит на `scatterAt(id)`
— как летит триггер рядом, — а заглушка перестаёт быть единственным, чем эти карты можно показать.
Альтернатива — решить, что автоматическое разрушение не показывается покартно вовсе, и тогда такт
не должен раздавать этим картам полёт в кучу.

**Статус.** `времянка`, и уже наполовину меньше, чем была: одиночный релиз садится на настоящую
позу кучи, а без разброса летят только те две карты, которым куча честно не даёт места. Строка в
реестре находок на странице аудита (`apps/playground/stories/AnimationAuditStory`) заведена, статус
`open`.
